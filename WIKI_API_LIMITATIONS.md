# Placeholder

Wiki content is usually stored in a separate GitHub wiki repository (`<repo>.wiki.git`) rather than in the main repo, and this environment has API access only to the main repository contents, not to the wiki storage. That means these wiki pages cannot be programmatically created from here via GitHub’s wiki API.
